# Execution status

> **Agents:** Update every session. Read [DESIGN-PRINCIPLES.md](DESIGN-PRINCIPLES.md) before coding.

## Configuration

| Key | Value |
|-----|-------|
| `IPC_ROOT` | `ipc` |
| `PLANS_ROOT` | `robotics-ipc-module` |
| Baseline tag / commit | `243ede0d905e0e5365073b83feff06ba3427db07` (initial commit, `main`, `github.com/sbow/RoboticsIpcModule`) |
| Target platforms | Jetson (embedded), x86+CUDA (dev), HIL/sim (UDP) |

## Current phase

**Active:** `F` — Interoperability bridges (F1 + F2 complete + parked C5 Scopes A + B closed; F3 Node gateway next)

## Phase completion

| Phase | Name | Status | Notes |
|-------|------|--------|-------|
| A | Module packaging | `[x]` | Vendored ipc tree from sbow/cpp_tricks; flat layout `ipc/`; MODULE.md + ADR 0004 + lifecycle split + kRouterFrameVersion = 1 |
| B | Message & config | `[x]` | toml++ vendored; topology_loader + 4 profiles; ADR 0005 sideband; RouterLogFn; LastValueCache; unit tests 71/71 |
| C | Transport hardening | `[x]` | `ShmSpsc::try_send` + drop-on-full + `ShmRouterMetrics`; `idle_sleep_us` cuts idle CPU 100% → 1.63%; ADRs 0006 + 0007; SHM benchmark interruptible; `IPC_SKIP_SHM` default retired |
| D | Validation & stress | `[x]` | D0 (per-peer ring sizing, ADR 0009) + D1 (unit suites, self-routing rejection, SourceSeqTracker) + D2 (4 integration scenarios + D2a per-peer drop attribution) + D3 (soak / leak-check / idle-CPU scripts) + D4 (fault injection + datagram metrics) + CI workflow (GitHub Actions PR gate, `make ci` local mirror) |
| E | Robotics integration | `[x]` | E1 reference layout + E2 systemd units + E3 Phase-F bridge scaffolding + E4 timestamp ADR (CLOCK_MONOTONIC_RAW, cross-host delegated) |
| F | Interoperability bridges | `[ ]` | Python, Node, MAVLink, vision; also hosts eventfd idle-wake follow-up (ADR 0007 deferral) |
| G | Declarative routing | `[planned]` | Per-topic dispatch (former C5 Scope C, promoted 2026-05-30). New ADR + `RouteRule` surgery + all profiles + integration tests rebuilt. See [plans/G-declarative-routing.md](plans/G-declarative-routing.md). Not yet active; opens after Phase F closes |

### Phase A deliverables

- [x] A1 `ipc/MODULE.md` (platforms, include graph, link flags, thread model, shutdown contract, public/example boundary, known limitations)
- [x] A2 Public vs example split (carved `router/lifecycle.hpp` from `router_app.h`; `node.hpp` no longer leaks `#include "router_app.h"`; grep gate documented)
- [x] A3 `kRouterFrameVersion = 1` added to `ipc/src/router/frame.hpp` with frozen-v1 layout comment
- [x] A4 `docs/adr/0004-robotics-module-boundaries.md` (in/out scope, module API promise, bridges deferred to Phase F)

### Phase B deliverables

- [x] B1 `docs/adr/0005-payload-policy-and-sideband.md` + `ipc/src/router/sideband.hpp` (control plane vs sideband; SidebandHeader 16 B v1 with magic `'RSB1'`; `/robot_<peer>_<class>` naming convention; class constants for vision_nv12 / vision_jpeg / ml_tensor_in / ml_tensor_out / mavlink_bulk)
- [x] B2 `ipc/src/router/topology_loader.hpp` (TOML 1.0 via vendored toml++ v3.4.0) + `config/profiles/{x86_dev,jetson_prod,hil,sim_cloud}.toml`; `--config <path>` CLI on `router_server`; address grammar `<uds|udp|shm>:<rest>`; LoadedTopology owns string storage in `std::deque<std::string>` (move-safe on Linux); validates duplicate ids, scheme, length, route → peer references; 45/45 loader assertions pass
- [x] B3 `RouterLogFn` typedef + `router_set_log_fn()` in `router_app.h`; demos register `demo_stderr_logger` with `[info]/[warn]/[err]` tags; library routing remains string-free on hot path
- [x] B4 `ipc/src/router/last_value_cache.hpp` (`LastValueCache<N=256>`, subscriber-side, thread-unsafe by design); 26/26 cache assertions pass

### Phase C deliverables

- [x] C1 SHM `try_send` / bounded wait — `ShmSendResult { Ok, Full }` + `shm_try_push_slot` + `ShmSpsc::try_send`; `ShmRouterLink::send_to_peer` drops on Full instead of spinning; client→router blocking publish documented as a known limitation in MODULE.md (separate ADR future). Echo benchmark client now uses interruptible try_send+try_recv path; `IPC_SKIP_SHM` default removed from `make test-ipc`.
- [x] C2 Idle wake — `RouterRunOptions::idle_sleep_us` (default 1 ms) replaces unconditional `yield()`. Measured idle CPU dropped from 100% → 1.63% of one core over 60 s on `jetson_prod.toml`. `eventfd` path documented and deferred in [ADR 0007](../docs/adr/0007-router-idle-wake.md).
- [x] C3 Router metrics — `ipc/src/router/metrics.hpp` with `ShmRouterMetrics { forwarded, dropped_full, recv_empty, recv_truncated }`; `ShmRouterLink::metrics()` returns a stable reference (heap-allocated; link stays movable). See [ADR 0006](../docs/adr/0006-shm-backpressure-and-metrics.md).
- [x] C4 Datagram seq — **closed in Phase D1 / D2** by `ipc/src/router/source_seq_tracker.hpp` (per-source uint32 tracker with 2³² wrap-aware classification) + `burst_sensor_test` (5000-frame burst with `samples + gaps == seq_range` invariant under right-sized rings). Originally deferred; the D-phase consumer materialised on schedule.

### Phase D deliverables

- [x] D0 Per-peer SHM ring sizing — ADR 0009; `[[peers]] shm_slot_count` / `shm_max_payload` optional fields parsed and validated (≥ `kRouterFrameSize`, ≤ 256 MiB, ≤ 2²⁰ slots, only on `shm:` peers); `PeerEntry` carries sentinel-zero `uint32_t` overrides (aggregate-init compatible); new `bind_shm_endpoint(IpcEndpoint<ShmSpsc>&, const PeerEntry&, bool)` overload wires overrides through `ShmRouterLink`; `jetson_prod.toml` shipped with the recommended `256 × 64` per peer (~15× memory reduction vs. legacy 256 × 1024); `topology_loader_test` extended (+26 assertions). No source change in any compile-time consumer; `make test-router` green on all three transports.
- [x] D1 Unit tests — `datagram_seq_test`, `routing_test`, `resolver_test`, `cli_args_test` + topology-loader self-routing rejection; new library header `router/source_seq_tracker.hpp` + test-only `router_cli_args.hpp` helper; **8 unit binaries / 642 assertions** aggregate
- [x] D2 Integration — `slow_recorder_test` (D2a per-peer drop attribution gate), `burst_sensor_test` (closes deferred C4 via `SourceSeqTracker`), `profile_switch_test` (jetson_prod SHM ↔ hil UDP round-trip), `router_restart_test` (SIGKILL + re-bind); **4 binaries / 64 assertions**. D2a: `ShmRouterMetrics::dropped_full_per_peer[256]` (ADR 0006 closed-loop), aggregate counter preserved
- [x] D3 Stress/soak — `robotics-ipc-module/scripts/{soak_router,shm_leak_check,idle_cpu_check,latency_histogram}.sh` + `make test-soak` / `test-leak-check` / `test-idle-cpu` / `test-latency-histogram`; `SOAK_ITERATIONS=10` default; leak check globs `/dev/shm/rim_*` + `/tmp/rim_*.sock` and asserts delta == 0 across full unit + integration + router pass; idle-CPU gate at ≤ 5% (post-fix baseline 1.5–1.7%) for the ADR 0007 regression
- [x] D4 Fault injection — `ipc/test/fault_injection_test.cpp` (34 assertions, 6 scenarios: truncated UDP, unknown-source UDP, wrong UDS path, UDS rebind after stale socket, `shm_max_payload < kRouterFrameSize` loader reject, SIGKILL mid-traffic SHM recovery). New `DatagramRouterMetrics { forwarded, recv_truncated, recv_unknown_source, recv_empty }` heap-owned by `DatagramRouterLink<T>` mirrors the SHM counter surface (ADR 0006 update) — closes the "datagram links have no metrics" limitation from MODULE.md
- [x] CI workflow (PR gate) — `.github/workflows/ci.yml` on every push to `main` + every PR + `workflow_dispatch`; runs `apt install build-essential ccache sysstat` → ccache restore (keyed on `Makefile` hash, OS-prefix fallback) → `make all` → `make test-ipc-unit` → `make test-ipc-integration` → `make test-router` → `shm_leak_check.sh` → `ccache --show-stats` → `actions/upload-artifact build/ipc/test/` on failure (7 day retention). `concurrency: cancel-in-progress` short-circuits superseded runs; `timeout-minutes: 5` ceiling. Local mirror via `make ci` (serialized sub-makes so `-jN` builds in parallel but tests don't race on `/dev/shm/rim_router_*` paths). Aggregate gate: 652 unit + 98 integration + UDS/UDP/SHM router + leak check, end-to-end ~32 s on the dev box (well under the 3-min PR-gate budget)

### Phase E deliverables

- [x] E1 Reference layout — `docs/robotics-reference-layout.md` (peer catalog with per-peer RouterFrame field contract + implementation status; topology mermaid + 4-profile pointer table; per-deployment shapes for Jetson on-robot / x86 dev / HIL bench / sim cloud; integration patterns for `vision_capture` / `ml_inference` / `mavlink_gateway` / recorder / python_peer / node_gateway; operational notes covering cross-host time / supervision / idle CPU / restart / cleanup; explicit forward refs to E2/E3/E4, Phase F F1–F5, and the post-phases robotics-integration review C1–C10). `ipc/MODULE.md` Related documents updated to link the new layout. No code changes; `make all` and `make test-router` green.
- [x] E2 systemd (Jetson-oriented) — `robotics-ipc-module/deploy/systemd/` ships `rim-router.service` (Type=simple, `ExecStart=router_server --config /etc/rim/router.toml`, `ExecStopPost=rim-router-cleanup.sh`, `TimeoutStopSec=5s`, `Restart=on-failure`, sandboxing via `NoNewPrivileges=` + `ProtectSystem=strict` + `ReadWritePaths=/dev/shm /tmp /run/rim`, journald wired, hardening directives `MemoryLock=` / `CPUAffinity=` / `LimitRTPRIO=` commented out with explicit pointer to parked review C7), `rim-peer@.service` template (`%i` = role; `After=`+`Requires=rim-router.service`; per-instance overrides via `EnvironmentFile=-/etc/rim/peer-%i.env`; default `RIM_TRANSPORT=shm` matching jetson_prod profile; `Restart=on-failure RestartSec=500ms` as the coarse fallback while parked C6 readiness signaling remains deferred), `rim-router-cleanup.sh` (idempotent `rm -f` of `/dev/shm/rim_router_*` + anticipated sideband regions `rim_vision_*` / `rim_ml_*` + `/tmp/rim_router_*.sock`; preserves `/tmp/rim_router_*.log` for post-mortem), and `README.md` (install recipe `/opt/rim/{bin,libexec,docs} + /etc/rim/`, profile selection, drop-in customization pattern, non-root user setup, logging via journald, cleanup behavior across clean stop vs SIGKILL, known limitations table cross-linked to parked C6 / C7 / C10 / Phase F sketches, acceptance gate). `systemd-analyze verify` passes with only the expected "path does not exist" warnings for `/opt/rim/...` pre-install. `docs/robotics-reference-layout.md` Jetson section + supervision note + forward-references table updated to link the new files; `ipc/MODULE.md` Related documents links `deploy/systemd/README.md`.
- [x] E3 Bridge pointers to Phase F — `examples/bridges/` scaffolded with five README files: index (`bridges concept + Phase F roadmap + principles from ADR 0004 + connection-transport table + layout convention + cross-refs to E1 / F plan / ADRs 0004 / 0005 / 0008 / parked review`), `python_peer/` (F2 stub linking E1 contract + F2 plan + ADR 0004 boundary on no `Python.h` / no `pybind11` in libipc), `node_gateway/` (F3 stub on no `node.h` in libipc + separate-process pattern + npm not gating C++ build), `mavlink_gateway/` (F4 stub on no `mavlink.h` in libipc + dialout supplementary group + optional `mavlink_bulk` sideband + serial device permissions), and `vision_peer/` (F5 stub on no GStreamer / V4L2 / NVMM headers in libipc + pixel data in sideband not inline + bridge owns `/dev/shm/rim_vision_*` lifecycle + cross-refs to parked C1 / C2 / C9). All five integration-pattern subsections of `docs/robotics-reference-layout.md` updated: status lines now link the per-bridge stub directories alongside the F plan anchors; the integration-patterns intro adds a forward pointer to `examples/bridges/`; forward-references table E3 row points at `examples/bridges/`. `ipc/MODULE.md` Related documents links `examples/bridges/README.md`. **No code in any subdirectory yet** — Phase F sessions replace each stub README with the actual implementation. **No build target** for `examples/bridges/` in the top-level Makefile; bridge builds intentionally stay separate so missing Python / Node / GStreamer toolchains never gate the C++ workflow.
- [x] E4 Monotonic/PTP timestamp ADR — `docs/adr/0010-router-timestamp-clock.md` records the decision to stamp `RouterFrame::timestamp_ns` with `CLOCK_MONOTONIC_RAW` (slew-free, restart-surviving within a boot epoch, single-host cross-process comparable). New library helper `ipc/src/router/timestamp.hpp` provides `inline uint64_t router_now_ns()` wrapping `clock_gettime(CLOCK_MONOTONIC_RAW)` — peers that stamp their own frames include this header. `ipc/test/router_server.cpp` updated: dropped `kStartTime` global + `ns_since_start` function + `<chrono>` include; now passes `router_now_ns` as the `server.run` clock callback. Wire format unchanged (no `RouterFrame` carve-up). Cross-host time correlation is explicitly **delegated** per ADR 0010 §Out of scope: applications add their own clock field in the 32 B inline payload or in a sideband region for HIL / sim_cloud needs; a future dedicated recorder module (the kind needed for replay per parked review C4) is the natural home for richer time semantics (PTP, capture-time, host-clock skew tracking) because its latency / statelessness requirements are relaxed relative to the router's hot path. Parked review C8 single-host portion is now closed; cross-host portion remains in the backlog as a delegation policy, not a deferred deliverable. Alternatives considered and rejected: `steady_clock` (status quo — implementation-defined, slew-prone), `CLOCK_TAI` + PTP (drags host dependency into router scope), two-clock frame (would re-carve ADR 0008 layout), `CLOCK_BOOTTIME` (semantic overlap with no robotic-deployment benefit). Cross-links: `docs/robotics-reference-layout.md` "Cross-host time" section rewritten to "Time semantics" with ADR 0010 properties + explicit delegation policy; scope-vs-deferred table row updated; vision_capture integration pattern's `timestamp_ns` row points at the new clock. `ipc/MODULE.md` Wire-format table row + Related documents both link ADR 0010 + `router/timestamp.hpp`.

### Phase F deliverables

- [x] F1 Profile templates + `deployment-profiles.md` — All four `config/profiles/*.toml` files expanded from 3 peers to 6 (1 sensor / 2 controller / 3 recorder / 4 vision_capture / 5 ml_inference / 8 dashboard_feed), matching the [8-peer catalog](SYSTEM-VISION.md) 1:1 (peers 6 mavlink_gateway and 7 python_tooling reserved for F4/F2; port 19106/19107 + address 10.0.0.7/.8 left vacant). `jetson_prod.toml` ships all-SHM at `256 × 64` per peer (ADR 0009); sideband regions moved from the controller placeholder onto their real owners — `[[peers.sideband]]` for `/rim_vision_nv12` (8 MiB) on peer 4, plus `/rim_ml_tensor_in` (16 MiB) and `/rim_ml_tensor_out` (4 MiB) on peer 5 — with `rim_` prefix per the rename ADR. Routes wired within the 2-dest-per-source cap: 1→[2,3], 2→[3], 4→[5,3], 5→[2,3]; recorder (3) is the central log+tap; dashboard_feed (8) has no inbound route in F1 (documented limitation). New operator-facing companion doc `docs/deployment-profiles.md` (~270 lines): profile selector + per-profile shape (all 4 with sideband + port allocation tables) + route topology mermaid + **Known limitations** section documenting (a) "single transport per router instance" — `ShmRouterLink::bind_router` silently skips non-SHM peers and `send_to_peer` throws on unknown dests, so mixing SHM+UDS in one profile crashes the forward loop on the first cross-transport forward; this is the realization that drove jetson_prod back to all-SHM, and the proper fix is parked C5 mixed-transport routing; (b) "2-destination cap" — `RouteRule.dest0/dest1` + first-match-wins means dashboard can't subscribe-all in a single rule; also parked C5 — plus resource-name convention table and operator hand-off checklist. Test updates: `topology_loader_test::test_load_from_file` asserts 6 peers + 4 routes (was 3 + 2) and peer-id-presence (1/2/3/4/5/8 present; 6/7 absent); `test_shm_ring_sizing_jetson_profile` asserts 6 SHM peers all at 256×64; SHM cleanup lists in `profile_switch_test.cpp` + `router_restart_test.cpp` + `fault_injection_test.cpp` extended with `rim_router_vision_capture` / `rim_router_ml_inference` / `rim_router_dashboard`; `smoke_jetson_round` + `smoke_hil_round` peer_count assertions updated from 3 to 6. Cross-links: `docs/robotics-reference-layout.md` per-deployment profile mapping table + scope-vs-deferred table forward-pointer to `deployment-profiles.md`; `ipc/MODULE.md` Deployment profiles section retitled "Phase F F1", rewritten to mention the 6-peer expansion + Known limitations + cross-link, Related documents updated; `examples/bridges/README.md` connection-transport table + Related documents link the new doc. Acceptance gate (`make ci`): 652 unit + 110 integration + UDS/UDP/SHM router + `shm_leak_check.sh` clean — 0 leaked rim_* resources across 28.6 s end-to-end; assertions changed from 89→91 in topology_loader and 11→13 in profile_switch from the F1 added expectations.
- [x] F2 Python bridge example — `examples/bridges/python_peer/` (stdlib-only, no third-party deps): `rim_router_frame.py` ports the 64 B `RouterFrame` v2 layout via `ctypes.LittleEndianStructure` with `_pack_ = 1`, field-offset asserts that fail-fast at import time if any of the 9 offsets drift from `ipc/src/router/frame.hpp`, an `int` property for the 6-byte `sideband_len` (u48), `RouterFrame.make(...)` convenience constructor + `RouterFrame.from_bytes` / `to_bytes` round-trip, `payload_bytes()` matching the C++ rstrip-NUL convention, and a `RouterFrameView` frozen dataclass for downstream consumers; `rim_router_peer.py` provides `RouterPeer` wrapping one `AF_UNIX SOCK_DGRAM` socket — binds the peer-7 path before sending (router resolves source by sender's `sun_path` via `peer_id_from_recv`; unbound senders get counted as `recv_unknown_source` per Phase D4), unlinks-stale-then-bind to match `Uds::bind` semantics, context-manager cleanup, `send_frame` / `publish(payload, ...)` / `recv_frame()` (truncated < 64 B datagrams dropped silently, matching the router's `recv_truncated` policy), and `router_now_ns()` helper exposing `CLOCK_MONOTONIC_RAW` per ADR 0010. CLI scripts: `subscribe.py` (binds peer path, decodes via ctypes, prints per-frame source-name resolved from peer catalog, exits on `--count` or SIGINT/SIGTERM) and `publish.py` (configurable count / interval / payload / topic-id, monotonic seq, router-clock timestamps). End-to-end smoke `smoke.sh` writes a minimal 2-peer UDS profile under `build/python_peer_smoke/`, starts router with `ROUTER_TEST=1` so it self-terminates on idle (no kill signals — important in sandboxed CI), runs `Python publish → C++ router → Python subscribe` round-trip, asserts router log shows N forward lines + subscriber decoded N frames + each `seq` carries the expected `python-tooling-N` payload byte-for-byte. Profile updates appending peer 7 (`python_tooling`): `x86_dev.toml` UDS at `/tmp/rim_router_python_tooling.sock`; `jetson_prod.toml` SHM at `/rim_router_python_tooling` (all-SHM constraint — UDS Python bridge therefore not directly reachable from Jetson today; parked C11); `hil.toml` UDP port 19107; `sim_cloud.toml` UDP `10.0.0.8:19207`. Routes extended within the 2-dest-per-source cap: `source = 7 dest = [3]` (python publishes to recorder) + `source = 2 dest = [3, 7]` (controller tap into python_peer). Tests updated to follow: `topology_loader_test.cpp` `test_load_from_file` now asserts 7 peers + 5 routes + `peer_by_id(view, 7) != nullptr`; `test_shm_ring_sizing_jetson_profile_*` asserts 7 SHM peers; `profile_switch_test.cpp` `smoke_jetson_round` / `smoke_hil_round` both bumped to `peer_count == 7u`; SHM cleanup arrays in `profile_switch_test.cpp` / `router_restart_test.cpp` / `fault_injection_test.cpp` extended with `/rim_router_python_tooling`. Cross-links updated: `examples/bridges/README.md` peer-status row flipped to **Implemented**; `docs/robotics-reference-layout.md` peer-catalog row + integration-pattern section now describe shipped behavior (UDS only in F2; UDP variant a one-method change; ADR 0010 timestamp-rewrite note explicit); `docs/deployment-profiles.md` peer catalog (7 peers), route topology mermaid (adds peer 7 + tap edges), TOML snippet (adds two new routes), per-profile hil-port + sim_cloud-address tables, and forward-references all updated; `ipc/MODULE.md` Deployment-profiles section bumped to "F1 expanded to 6; F2 added peer 7", new bullet under Related documents linking the python_peer directory. Acceptance: `make all` + `make test-router` + `make test-ipc-unit` + `make test-ipc-integration` all green (94/94 topology, 43/43 profile switch, 17/17 restart, 34/34 fault injection, all 3 router transports); `examples/bridges/python_peer/smoke.sh` green — `[smoke] OK — Python <-> C++ wire format parity verified` with 5 frames forwarded and 5 decoded byte-exact.
- [ ] F3 Node gateway example
- [ ] F4 MAVLink gateway sketch
- [ ] F5 Vision metadata peer sketch

## Open questions / backlog

- Post-phases revisit: see [plans/post-phases-robotics-review.md](plans/post-phases-robotics-review.md) — 11 considerations (TensorRT contract, CUDA `memory_class`, ARM verification, x86 playback, declarative transport, systemd readiness, RT pinning, cross-host time, camera shape, consumption model, mixed-transport networks) deferred until Phases E and F close. C11 (mixed transport) was added during F1. **C5 Scopes A + B closed early during Phase F (2026-05-28)** — 2-destination cap lifted; declarative `[[topics]]` registry added. **Scope C promoted to [Phase G — Declarative routing](plans/G-declarative-routing.md) 2026-05-30** (per-topic dispatch is phase-shaped work: new ADR, real `RouteRule` surgery, all profiles touched, integration tests rebuilt — not close-out work). **Scope D merged into C7** (RT pinning, mlockall, SCHED_FIFO + priority-aware QoS — both are latency-under-contention problems sharing the same hot path and systemd-directives surface).

## Blockers

_None._

## Session log

| Date | Phase | Summary | Agent/human |
|------|-------|---------|-------------|
| | | Plan pack created | initial |
| | | Added DESIGN-PRINCIPLES, LESSONS-LEARNED, SYSTEM-VISION, Phase F | plan update |
| 2026-05-25 | — | Repo init + push to `github.com/sbow/RoboticsIpcModule`; baseline `243ede0` recorded | orchestrator |
| 2026-05-25 | A | Vendored `ipc/` + `docs/adr/0001-0003` from `sbow/cpp_tricks` (plain copy); flat IPC_ROOT=`ipc`; fresh top-level `Makefile` (test-ipc skips SHM until Phase C, test-router runs all 3 transports) | phase-a |
| 2026-05-25 | A | A1 MODULE.md; A2 lifecycle split (`router/lifecycle.hpp`) removes `node.hpp` → `router_app.h` leak; A3 `kRouterFrameVersion = 1`; A4 ADR 0004 — `make all && make test-ipc && make test-router` all green | phase-a |
| 2026-05-25 | B | Vendored toml++ v3.4.0; B1 ADR 0005 + `router/sideband.hpp`; B2 `router/topology_loader.hpp` + 4 profiles + `router_server --config <toml>` (auto-derives transport from listen kind); B3 `RouterLogFn` plug-in + leveled `router_log`; B4 `router/last_value_cache.hpp`; `make test-ipc-unit` adds topology + cache tests (71/71 assertions); `make test-ipc` + `make test-router` regression green | phase-b |
| 2026-05-25 | C | **Baseline measurement (yield-only):** idle CPU = 100% one core sustained over 60 s (`pidstat -p $PID 10 6`, jetson_prod profile, no clients). C1 `ShmSendResult` + `shm_try_push_slot` + `ShmSpsc::try_send`; `ShmRouterLink::send_to_peer` drops on Full and bumps `dropped_full` (no infinite spin). C3 `router/metrics.hpp` with `ShmRouterMetrics` (atomics, heap-owned via `unique_ptr`, link stays movable); `metrics()` accessor stable for link lifetime. C2 `RouterRunOptions::idle_sleep_us` (default 1 ms) replaces unconditional `yield()`. **Post-fix measurement:** idle CPU = 1.68% one core averaged over 60 s (same host, same profile) — comfortably under the 5% acceptance bar. ADR 0006 (drop-on-full + metrics) and ADR 0007 (idle-wake sleep_for now, eventfd deferred to Phase F). Echo client SHM path made interruptible via try_send + try_recv + stop checks; `IPC_SKIP_SHM=1` default removed from `make test-ipc`. New unit test `ipc/test/shm_backpressure_test.cpp` (3 scenarios, 27/27 assertions) verifies forwarded / recv_empty / drop policy under ring saturation (published=1024 → forwarded=256, dropped_full=768; 2s deadline guard catches regressions). Aggregate: `make test-ipc-unit` 98/98 assertions, `make test-ipc` UDP 828k + UDS 890k + SHM 7.9M trips/5s, `make test-router` all 3 transports green. | phase-c |
| 2026-05-25 | C→D bridge | **ADR 0008 — RouterFrame v2.** Co-authored decision to retire v1 (32 B, 9 B big-endian timestamp, 22 B payload, no topic / seq / sideband descriptor) in favor of a 64 B cache-line-sized header: `source`, `flags` (has_sideband/keyframe/is_ack/eos/priority), `topic_id`, `seq`, `timestamp_ns` (host LE), `sideband_idx`, `sideband_len` (uint48, 256 TB cap), `sideband_seq`, 32 B inline payload. `kRouterFrameVersion` bumped 1→2. Single-deploy migration (no on-wire negotiation); existing in-tree callers (`router_client.cpp`, `router_server.cpp`, `router_test.cpp`, all unit tests) compiled unchanged thanks to API-compatible accessors. New unit test `ipc/test/frame_test.cpp` covers every field + offset + uint48 truncation + neighbor-non-clobber (163/163 assertions). ADR 0004 updated to point at v2 as active contract (v1 layout preserved for archeology); ADR 0005 refined with the in-frame sideband descriptor and a forward declaration of `[[peers.sideband]] memory_class` for Phase F (CUDA/NvBufSurface). Aggregate post-v2: `make test-ipc-unit` 261/261 assertions, `make test-ipc` UDP 734k + UDS 825k + SHM 8.6M trips/5s, `make test-router` all 3 transports green. | phase-c→d |
| 2026-05-25 | D0 | **ADR 0009 — Per-peer SHM ring sizing.** `[[peers]]` schema gains optional `shm_slot_count` (default 256) and `shm_max_payload` (default 1024, must be ≥ `kRouterFrameSize` when set), validated at load time. `PeerEntry` grows two `uint32_t` trailing fields with in-class default `= 0` (sentinel for "use `ShmSpsc::BindParams` defaults"); all existing aggregate initializers (`kDemoPeers` in `router_client_config.h`, `kPeers` in `shm_backpressure_test.cpp`) compile unchanged. New `bind_shm_endpoint(IpcEndpoint<ShmSpsc>&, const PeerEntry&, bool)` overload threads the overrides through `ShmRouterLink::bind_router` and `bind_peer`. Loader rejects: `shm_max_payload < 64`, `shm_max_payload > 256 MiB`, `shm_slot_count > 2²⁰`, or any `shm_*` field set on a non-SHM peer — each with frame-aware error text. `jetson_prod.toml` updated to demonstrate the recommended sizing on all three peers (256 × 64 = ~35 KiB per ring direction; ~15× reduction vs. the legacy 256 × 1024 ≈ 514 KiB). `topology_loader_test` extended with 5 new scenarios (defaults preserved, overrides parsed, cache-footprint ratio asserted, validation matrix, jetson_prod profile invariant). Aggregate post-D0: `make test-ipc-unit` 287/287 assertions (163 + 71 + 26 + 27); `make test-router` all 3 transports green; `make test-ipc` UDP 745k + UDS 866k + SHM 8.2M trips/5s (no regression). | phase-d0 |
| 2026-05-25 | D2 | **Integration scenarios + per-peer drop attribution (D2a).** D2a extends `ShmRouterMetrics` with `std::array<std::atomic<uint64_t>, 256> dropped_full_per_peer` (indexed by destination peer id; ~2 KiB per metrics block, heap-owned via the existing `unique_ptr` so `ShmRouterLink` stays movable). `ShmRouterLink::send_to_peer` now bumps both the aggregate `dropped_full` (Phase C compatibility) and the per-destination slot on Full; verified by `shm_backpressure_test::test_per_peer_attribution_isolates_slow_destination` (sensor → {B drained, C ignored} fanout: drop_to_B=0, drop_to_C=768, per-peer counters sum to aggregate). ADR 0006 "Negative" → "Closed" for per-peer attribution. Four new D2 integration binaries (each follows the lightweight EXPECT/EXPECT_EQ pattern; no test framework deps): `slow_recorder_test` (in-process: 2 kHz sensor / 200 Hz recorder / full-rate controller — controller=2000/2000, recorder=261/2000, drop[ctrl]=0, drop[rec]=1523, attribution gate green); `burst_sensor_test` (closes the deferred C4 deliverable: 5000-frame burst with retry-on-full publish, subscriber drains via real `ShmRouterLink` + `SourceSeqTracker`, asserts `samples + gaps == seq_range` invariant + out_of_order=0 + duplicates=0 + ≥99% delivery; measured 5000/5000 with zero gaps under right-sized rings); `profile_switch_test` (loads `jetson_prod.toml` SHM → `hil.toml` UDP → `jetson_prod.toml` again, one round-trip per profile, validates ADR 0009 per-peer sizing carries through the loader); `router_restart_test` (subprocess: spawns `router_server --config jetson_prod.toml`, SIGKILLs it, asserts SHM regions linger then a fresh spawn binds cleanly via `shm_open`+`ftruncate`+`memset`; also covers overlapping spawns). Aggregate post-D2: `make test-ipc-unit` **652/652 assertions** across 8 binaries (added 10 D2a assertions to shm_backpressure); `make test-ipc-integration` **64/64 assertions** across 4 new binaries (11 + 9 + 27 + 17). `make test-router` UDS / UDP / SHM all green; `make test-ipc` UDP 724k + UDS 802k + SHM 7.7M trips/5s (no regression). | phase-d2 |
| 2026-05-26 | D4 | **Fault injection + datagram link metrics.** New library struct `DatagramRouterMetrics` in `ipc/src/router/metrics.hpp` mirrors `ShmRouterMetrics`'s observability surface for UDP / UDS — `forwarded` (one increment per destination per frame), `recv_truncated` (datagram < `kRouterFrameSize` dropped), `recv_unknown_source` (recvfrom() peer not in topology, dropped), `recv_empty` (parity placeholder; datagram recv() throws on SO_RCVTIMEO so the outer `RouterServer::run` loop catches it instead — driven counter deferred to a later phase). `DatagramRouterLink<T>` (`ipc/src/router/link.hpp`) gains a `std::unique_ptr<DatagramRouterMetrics> metrics_` member + `metrics()` accessor (same Phase C3 movability pattern as `ShmRouterLink`), increments the three driven counters at the three return paths in `forward()`. Zero changes to call sites — existing demos and integration tests build clean. New integration binary `ipc/test/fault_injection_test.cpp` exercises six fault scenarios with 34 assertions total: (1) **truncated UDP datagram** — sender at peer port pushes 16-byte and 8-byte buffers → `recv_truncated == 2`, plus one valid 64-byte frame → `forwarded == 1`; (2) **unknown-source UDP** — sender bound to port 25099 (not in topology) sends 2 frames, sender bound to the registered sensor port sends 1 → `recv_unknown_source == 2`, `forwarded == 1`; (3) **wrong UDS path** — `bind_router("/nonexistent_dir_xyzzy/router.sock")` throws `runtime_error("uds bind failed")` cleanly with no partial state; (4) **UDS rebind after stale socket** — second `bind_router` on a path with leftover socket file from a prior crash succeeds (regression for `Uds::bind`'s `unlink-then-bind` sequence); (5) **shm_max_payload < kRouterFrameSize** — `load_topology_from_toml_string` rejects at parse time with frame-aware error message (cross-references topology_loader_test); (6) **SIGKILL mid-traffic** — spawns `router_server jetson_prod.toml`, pushes 16 SHM frames while the router is forwarding, SIGKILLs, asserts per-peer SHM rings persist on `/dev/shm`, spawns a second router_server on the same profile, verifies clean bind + accepts a fresh publish (complements `router_restart_test`'s idle-state SIGKILL coverage). Test is sandbox-safe: AF_INET socket failure → `[skip]` path; subprocess reaping is bounded via `reap_bounded()` helper that escalates SIGTERM → SIGKILL on a deadline (parent → child signal delivery has been observed to be flaky in sandboxed CI hosts). ADR 0006 updated to document the datagram counters. Makefile gains `test-fault-injection` phony target and the binary is aggregated into `test-ipc-integration`. Acceptance: `make test-ipc-unit` 652/652 (no change), `make test-ipc-integration` now **98/98 assertions** across 5 binaries (was 64/64 across 4); `make test-leak-check` clean (sandbox-safe); `make test-router` UDS / UDP / SHM all green; `make test-ipc` UDP 730k + UDS 808k + SHM 7.9M trips/5s (no regression). | phase-d4 |
| 2026-05-26 | D3 | **Stress / soak scripts.** Four shell scripts under `robotics-ipc-module/scripts/` plus matching `.PHONY` targets in `Makefile`. (1) `soak_router.sh [N=10]` loops `./build/ipc/test/router_test`, cleans `/dev/shm/rim_*` + `/tmp/rim_*.{sock,log}` between iterations, aborts on first non-zero exit, dumps the failed iteration's log to stderr, prints a per-iteration timing line, and ends with a `summary: passed=N/N total=Xms mean=Yms min=A max=B` + a CSV-ish `iterations_ms:` line for graphing. (2) `shm_leak_check.sh` counts `rim_*` resources under `/dev/shm/` and `/tmp/*.sock` before/after `make test-ipc-unit && make test-ipc-integration && make test-router`, asserts delta == 0; warns + auto-cleans pre-existing resources from interrupted prior runs so the baseline is honest; `LEAK_CHECK_SKIP_ROUTER=1` escape hatch for hosts where AF_INET is sandboxed. (3) `idle_cpu_check.sh` boots `router_server --config config/profiles/jetson_prod.toml` (with `ROUTER_TEST` unset so it stays alive), waits up to 3 s for `/dev/shm/rim_router_sensor` to appear, runs `pidstat -u -p $PID 10 6` (60 s window by default; knobs: `IDLE_CPU_SAMPLES`, `IDLE_CPU_INTERVAL`, `IDLE_CPU_THRESHOLD`), parses `%CPU` from the `Average:` line by header position (sysstat-version robust), asserts ≤ 5 % via `bc -l`, then SIGTERM → SIGKILL → SHM cleanup in the trap; **measured 1.50 % over a 3 × 2 s smoke run** (regression gate for ADR 0007 / Phase C2 yield → sleep_for fix). (4) `latency_histogram.sh [N=5]` (optional) repeatedly runs `echo_tests`, parses the `<TRANSPORT> round trips in 5s: <N>` lines, and prints a `min / p25 / p50 / p75 / max / max-over-min` table per transport — throughput variance rather than per-trip latency (per-trip needs a benchmark flag, deferred to Phase E). Makefile gains `test-soak`, `test-leak-check`, `test-idle-cpu`, `test-latency-histogram` phony targets; `SOAK_ITERATIONS=10` make-var lets callers override. Acceptance gate: `make test-leak-check` (no leaks across 753 unit + 64 integration + router assertions), `make test-idle-cpu` (≤ 5 % gate), `make test-soak SOAK_ITERATIONS=10` (10 × `router_test` clean). | phase-d3 |
| 2026-05-26 | D-CI | **CI workflow (PR gate) + local `make ci` mirror.** New `.github/workflows/ci.yml` runs on every push to `main` and every PR (plus `workflow_dispatch`) with `concurrency: cancel-in-progress` so fast pushes don't pile up runner minutes. Steps: `apt install build-essential ccache sysstat` → `actions/cache@v4` restore of `~/.ccache` keyed on `OS-ccache-<hash(Makefile)>-<sha>` with fallback to OS-prefix → `g++` shadowed via `/usr/lib/ccache` in `$GITHUB_PATH` (no Makefile change) → `make all` → `make test-ipc-unit` (652 assertions) → `make test-ipc-integration` (98 assertions) → `make test-router` (UDS / UDP / SHM scenarios) → `bash robotics-ipc-module/scripts/shm_leak_check.sh` (delta == 0 gate) → `ccache --show-stats` (always) → `actions/upload-artifact build/ipc/test/` with 7-day retention on failure. Hard `timeout-minutes: 5` ceiling. Not on the PR gate: `test-soak`, `test-idle-cpu`, `test-latency-histogram`, `test-ipc` (variance / runtime / informational — Phase E nightly candidate). New `make ci` target dispatches the same five stages as serialized sub-makes (`@$(MAKE) all; @$(MAKE) test-ipc-unit; ...`) so a `-jN` parent still parallelises the *build* but not the *tests* (a couple of D2/D4 binaries — `profile_switch_test`, `router_restart_test`, `fault_injection_test` — bind well-known `/rim_router_*` SHM names from `jetson_prod.toml` and would race for them otherwise). Two new build-only aggregates (`build-ipc-unit`, `build-ipc-integration`) keep the compile fan-out fast while the run aggregates iterate sub-makes for ordering. End-to-end `make -j$(nproc) ci` measured at 32 s on the dev box (clean rebuild + all 5 stages + leak check), comfortably under the 3-min runner budget. `ipc/MODULE.md` gains a new "Continuous integration (Phase D)" subsection documenting the workflow steps, budget, and what's intentionally out-of-scope. | phase-d-ci |
| 2026-05-26 | E3 | **Phase F bridge scaffolding.** New `examples/bridges/` directory with five README files (zero code) per the optional E3 deliverable. (1) `examples/bridges/README.md` (~75 lines) — bridges concept (separate processes speaking the v2 wire layout; libipc stays language-agnostic), status table (peer 4 / 6 / 7 / 8 → F5 / F4 / F2 / F3 deliverables, all marked "Sketch only"), ADR 0004 principles enumerated (no `Python.h` / `node.h` / `mavlink.h` in `ipc/src/`; same wire format / peer IDs / route rules as C++ peers; bulk data via sidebands per ADR 0005; no ROS unless a separate ADR adds it), connection-transport recommendation table (UDS on x86 dev, SHM on Jetson, UDP loopback on HIL, container subnet on sim_cloud), layout convention for when Phase F populates each subdirectory (`README.md` + `frame_layout.<ext>` + sources + isolated build script — top-level `make all` builds **only** the C++ core), rationale for scaffolding-now-implementation-later (reserve directory shape without committing to Python / Node / GStreamer library versions). (2) `python_peer/README.md` (~30 lines) — F2 stub: peer 7 contract pointer to E1 §Python bridge, F plan pointer to F2, ADR 0004 boundary (no `Python.h` / `pybind11` in libipc; bridge is a separate process, not in-process embedding). (3) `node_gateway/README.md` (~25 lines) — F3 stub: peer 8 contract pointer to E1 §Node dashboard gateway, F plan pointer to F3, ADR 0004 boundary (no `node.h` / N-API in libipc; `npm install` does not gate C++ build). (4) `mavlink_gateway/README.md` (~35 lines) — F4 stub: peer 6 contract pointer to E1 §MAVLink gateway, F plan pointer to F4 (the F plan acknowledges F4 may ship as README + ADR before any binary), optional `mavlink_bulk` sideband class per ADR 0005, dialout supplementary group note, SYSTEM-VISION.md cross-ref reminding that MAVLink stays the MCU contract (not replaced by `RouterFrame` on the wire). (5) `vision_peer/README.md` (~50 lines) — F5 stub: peer 4 contract pointer to E1 §Vision capture, F plan pointer to F5 (which includes the `memory_class` parser landing), explicit cross-refs to parked-review C1 (TensorRT contract depth) + C2 (CUDA `memory_class` parsing) + C9 (camera shape) so the Phase F session reads them before designing, ADR 0004 boundary (no GStreamer / V4L2 / NVMM in libipc; pixel data in sideband; bridge owns `/dev/shm/rim_vision_*` lifecycle while `rim-router-cleanup.sh` proactively `rm -f`s the anticipated names). Cross-links: all five E1 integration-pattern subsections (`vision_capture`, `ml_inference`, `mavlink_gateway`, recorder, `python_peer`, `node_gateway`) — status lines updated to link the per-bridge stub directory **and** the specific F-plan anchor; integration-patterns intro adds a forward pointer to `examples/bridges/`; forward-references table E3 row points at `examples/bridges/` (was Phase E E3 plan anchor). `ipc/MODULE.md` Related documents adds a link to `examples/bridges/README.md`. STATUS.md Phase E E3 → `[x]`; active phase advanced to "E1 + E2 + E3 complete; E4 next". Acceptance: `make all` clean (markdown-only — `examples/bridges/` is **not** in any Makefile target), `make test-router` UDS / UDP / SHM all green (no behavioral change). Parking discipline maintained: parked C1 / C2 / C9 surfaced explicitly in the vision_peer stub so the Phase F session walks them before committing to a memory_class parser or a TensorRT contract shape, but the review document itself is not modified. | phase-e3 |
| 2026-05-26 | E2 | **systemd unit examples.** New directory `robotics-ipc-module/deploy/systemd/` with four files: `rim-router.service` (main router unit — `Type=simple`, `ExecStart=/opt/rim/bin/router_server --config /etc/rim/router.toml`, `TimeoutStopSec=5s`, `ExecStopPost=/opt/rim/libexec/rim-router-cleanup.sh`, `Restart=on-failure RestartSec=1s`, sandboxing via `NoNewPrivileges=yes` + `ProtectSystem=strict` + `ReadWritePaths=/dev/shm /tmp /run/rim` + `ProtectHome=yes` + `PrivateTmp=no` because SHM/UDS paths live under those mountpoints, journald via `StandardOutput=journal` + `StandardError=journal`, production hardening directives `MemoryLock=infinity` / `CPUAffinity=2` / `LimitRTPRIO=80` / `Nice=-10` shipped commented out with explicit pointer to parked review C7 — router does not call `mlockall`/`sched_setscheduler` today, these are the recommended user-side wrapping), `rim-peer@.service` (template — instance `%i` is the peer role name `sensor` / `controller` / `recorder`; `After=rim-router.service` + `Requires=rim-router.service`; per-instance overrides via `EnvironmentFile=-/etc/rim/peer-%i.env` so operators tune transport without editing the unit; default `RIM_TRANSPORT=shm` matches jetson_prod profile; `ExecStart=router_client %i ${RIM_TRANSPORT} ${RIM_EXTRA_ARGS}`; `Restart=on-failure RestartSec=500ms` is the coarse fallback for the parked-C6 readiness race — peer code is expected to retry first connect with backoff until `sd_notify` lands), `rim-router-cleanup.sh` (POSIX `/bin/sh`, idempotent `rm -f` of `/dev/shm/rim_router_*` + anticipated sideband regions `rim_vision_*` / `rim_ml_*` + `/tmp/rim_router_*.sock`; preserves `/tmp/rim_router_*.log` for post-mortem; chmod 0755), and `README.md` (220-line operator guide — install recipe `/opt/rim/{bin,libexec,docs}` + `/etc/rim/`, profile selection table mapping jetson_prod/x86_dev/hil/sim_cloud → transport → use case, drop-in pattern via `systemctl edit` for hardening and per-instance transport overrides, non-root user setup with `useradd --system rim` + `install -d -o rim -g rim /run/rim`, logging via journald + the peer demo CSV at `/tmp/rim_router_<role>.log`, cleanup behavior on clean stop vs SIGKILL with explicit pointer to the `sudo pkill -KILL -f` recipe in LESSONS-LEARNED.md, known limitations table cross-linking parked C6 / C7 / C10 and Phase F sketches, acceptance gates). `systemd-analyze verify` on the unit files passes with only the expected pre-install warnings about `/opt/rim/bin/router_server` and `/etc/rim/router.toml` not existing (resolved once the operator completes the install recipe). Cross-links: `docs/robotics-reference-layout.md` Jetson process-layout table now links the unit files; the supervision note (formerly "Until E2 lands") now references the shipped directory; the forward-references table E2 row now points at `deploy/systemd/`; the scope-vs-deferred table updated to note `sd_notify` deferred beyond E2 (parked C6) since the units intentionally ship `Type=simple`. `ipc/MODULE.md` Related documents links `deploy/systemd/README.md`. STATUS.md Phase E E2 → `[x]`; active phase advanced to "E1 + E2 complete; E3 next". Acceptance: `make all` clean (no code changes), `make test-router` UDS / UDP / SHM all green (regression sanity), `systemd-analyze verify robotics-ipc-module/deploy/systemd/*.service` returns exit 0 with only path-existence advisories. | phase-e2 |
| 2026-05-26 | E1 | **Reference layout doc.** New `docs/robotics-reference-layout.md` (~290 lines) per Phase E E1 plan: purpose + scope-vs-deferred table (cross-references parked review for items not in E1), peer catalog (1–8) with per-peer **inline frame field use** + **sideband use** + **status today** (1–3 shipped demo binaries; 4–8 are Phase F sketches), topology mermaid (single-host router fanout + Phase F bridge subgraph), profile-pointer table (jetson_prod / x86_dev / hil / sim_cloud), per-deployment shapes covering transport, process layout, operational knobs (`MemoryLock=`, `CPUAffinity=`, `LimitRTPRIO=` documented as user-side systemd directives — none enabled in router today, see parked C7), cleanup recipes, and CI scope; integration patterns for `vision_capture` (contract surface on RouterFrame fields + sideband descriptor; class informational, `memory_class` deferred per parked C2 / ADR 0008), `ml_inference` (TensorRT engine is user code; subscriber decodes sideband_idx → sideband_seq; per-source routing limitation flagged per parked C5), `mavlink_gateway`, recorder (CSV format limitation flagged per parked C4), Python (F2 sketch — ctypes layout, no CPython in libipc per ADR 0004), Node (F3 sketch); operational notes (cross-host time forward-ref to E4 / parked C8; supervision forward-ref to E2; idle CPU per ADR 0007; restart per router_restart_test + fault_injection_test; cleanup via `pkill -KILL -f` + `shm_leak_check.sh`); forward references table mapping every deferred topic to E2/E3/E4/F1–F5/parked review. Cross-link added to `ipc/MODULE.md` Related documents. STATUS.md Phase E E1 → `[x]`; active phase advanced to E. Acceptance: `make all` clean (no code changes — markdown-only delivery), `make test-router` UDS / UDP / SHM all green (regression sanity, no change to runtime behavior). | phase-e1 |
| 2026-05-27 | F2 | **Python bridge example — ctypes RouterFrame port + UDS subscriber/publisher + end-to-end smoke.** Phase F F2 deliverable. New module under `examples/bridges/python_peer/`: (1) `rim_router_frame.py` — `ctypes.LittleEndianStructure` with `_pack_ = 1` mirroring the 64 B [`RouterFrame` v2](../../docs/adr/0008-router-frame-v2.md) layout byte-for-byte (fields `source` u8 / `flags` u8 / `topic_id` u16 / `seq` u32 / `timestamp_ns` u64 / `sideband_idx` u16 / `_sideband_len_bytes` `c_uint8 * 6` exposed as an `int` property for the u48 width / `sideband_seq` u64 / `payload` `c_uint8 * 32`); module-load asserts that `ctypes.sizeof(RouterFrame) == 64` and each of the 9 field offsets matches the C++ ADR 0008 table (fail-fast import error if the C++ struct drifts); helpers `RouterFrame.make(...)` convenience constructor + `from_bytes` / `to_bytes` round-trip + `payload_bytes()` matching the C++ rstrip-NUL convention + flag-bit accessors (`has_sideband`, `is_keyframe`, `is_ack`, `is_eos`, `priority()`) + frozen `RouterFrameView` dataclass for downstream consumers that should not hold raw ctypes buffers. (2) `rim_router_peer.py` — `RouterPeer` class wrapping one `AF_UNIX SOCK_DGRAM` socket; the router resolves source from the sender's bound socket path via `peer_id_from_recv` in `ipc/src/router/datagram_peer_resolver.hpp`, so the peer **must** `bind()` at the declared peer path before sending — `RouterPeer.__init__` unlinks-stale-then-binds (matching `Uds::bind` semantics in `ipc/src/ipc/datagram.hpp`), context-manager `__enter__`/`__exit__` for clean teardown, methods `send_frame(frame)`, `publish(payload, topic_id=, flags=, seq=, timestamp_ns=, priority=)` (priority validated 0..7), `recv_frame()` (returns `Optional[RouterFrame]`; truncated `< 64 B` datagrams dropped silently to match the router's Phase D4 `recv_truncated` policy on the server side), `set_recv_timeout(seconds)`. Also `router_now_ns()` helper exposing `CLOCK_MONOTONIC_RAW` via `time.clock_gettime_ns(time.CLOCK_MONOTONIC_RAW)` per [ADR 0010](../../docs/adr/0010-router-timestamp-clock.md) — peers stamp their own publish-time timestamps with the same clock the router uses. (3) `subscribe.py` — CLI: `--router-path`, `--peer-path`, `--peer-id`, `--count` (exit after N frames), `--quiet`; resolves source byte to peer name via a small catalog table; installs SIGINT/SIGTERM handlers for clean shutdown. (4) `publish.py` — CLI: configurable `--count` / `--interval-ms` / `--payload` (gets `-<seq>` suffix) / `--topic-id`; monotonic seq; stamps each frame with `router_now_ns()`. (5) `smoke.sh` — fully self-terminating end-to-end test: writes a minimal 2-peer UDS profile under `build/python_peer_smoke/` (peers 3 + 7 only, route `source = 7 dest = [3]`), starts `router_server --config <profile>` with `ROUTER_TEST=1` so the router self-exits on idle (no cross-process kill needed — important in sandboxes that disallow signal delivery across sandboxes), starts `subscribe.py` bound at the peer-3 path with `--count 5`, runs `publish.py` 5 frames as peer 7, then asserts the router log shows 5 `router: python_tooling -> recorder` lines, the subscriber decoded 5 frames, and **each `seq` carries the expected `python-tooling-N` payload byte-for-byte** — total smoke runtime ~2 s. (6) `README.md` (~170 lines) — quick start, smoke instructions with env tunables, wire-format contract table (offset / size / Python ctypes type / notes per field), explanation of how the router identifies a Python peer (bound `sun_path` resolution + Phase D4 `recv_unknown_source` fault gate), transport-per-deployment table (UDS reachable in F2; UDP one-method change; SHM Jetson not reachable without a separate UDS bridge daemon — parked C11), boundary policy (no `Python.h` / no `pybind11` / no in-process CPython per ADR 0004), known limitations (UDS only; no sideband decode; no JSON metadata path — all noted as the "optional" parts of the F2 plan), cross-references. Stdlib-only; no third-party Python deps; not wired into top-level `make all` (per [ADR 0004](../../docs/adr/0004-robotics-module-boundaries.md) — bridges build separately so a missing Python toolchain never breaks the C++ workflow). Profile updates appending peer 7 (`python_tooling`) to all four F1 profiles: `x86_dev.toml` adds UDS `/tmp/rim_router_python_tooling.sock`; `jetson_prod.toml` adds SHM `/rim_router_python_tooling` at `256 × 64` (all-SHM constraint; the Python bridge today is UDS-only so Jetson reachability is deferred to parked C11); `hil.toml` adds UDP port 19107 (the port that F1 already reserved); `sim_cloud.toml` adds UDP `10.0.0.8:19207`. Routes extended within the 2-destination-per-source cap: `source = 7 dest = [3]` (python_tooling publishes to recorder — F2 publish demo) + `source = 2 dest = [3, 7]` (controller tap into python_peer — F2 subscribe demo without sacrificing the recorder feed). Test updates to follow F1's "F2 will append peer 7" reservation: `topology_loader_test::test_load_from_file` bumped to expect 7 peers + 5 routes + `peer_by_id(view, 7) != nullptr` (was `== nullptr`); `test_shm_ring_sizing_jetson_profile_demonstrates_recommended_values` peer_count assertion bumped 6→7; `profile_switch_test::smoke_jetson_round` + `smoke_hil_round` `peer_count` assertions bumped 6u→7u; SHM cleanup arrays in `profile_switch_test.cpp` + `router_restart_test.cpp` + `fault_injection_test.cpp` extended with `/rim_router_python_tooling`. Cross-links: `examples/bridges/README.md` peer-status row for `python_peer` flipped from **Sketch only** to **Implemented**, transport-per-deployment table notes Jetson SHM caveat; `docs/robotics-reference-layout.md` peer-catalog row + "Python bridge" integration pattern subsection rewritten to describe shipped behavior + the bind-path-resolution detail + ADR 0010 timestamp rewrite note; `docs/deployment-profiles.md` peer catalog now lists 7 peers (peer 6 still pending F4), route mermaid + TOML snippet expanded with peer 7 + the controller tap + the python→recorder route, hil port table flips peer 7 from _Reserved for F2_ → Added in F2, sim_cloud address allocation note updated, peers-absent section narrowed to peer 6 only, resource-name table adds `rim_router_python_tooling` in both SHM and UDS rows, forward-references table flips Python bridge to **implemented**; `ipc/MODULE.md` Deployment-profiles section bumped to "F1 expanded to 6; F2 added peer 7", new bullet under Related documents linking the python_peer directory + describing the smoke. Acceptance gate: `make all` clean rebuild + `make test-ipc-unit` 94/94 topology assertions (was 91 — 3 new for peer-7 presence + 7-peer + 5-route counts) + `make test-ipc-integration` 110/110 (43/43 profile_switch, 17/17 router_restart, 34/34 fault_injection, all updated for the new region set) + `make test-router` UDS / UDP / SHM all green + `examples/bridges/python_peer/smoke.sh` green: `[smoke] OK — Python <-> C++ wire format parity verified` with 5 frames forwarded and 5 decoded byte-exact. Parking discipline maintained: parked C11 (mixed-transport networks) surfaced in the python_peer README and the jetson_prod.toml comment for the inevitable "but Python bridges on Jetson?" question, but the review document itself is not modified; C11 reopens during the post-Phase-F walk per the established discipline. | phase-f2 |
| 2026-05-27 | F1 | **Deployment profile templates (6 peers × 4 environments) + operator companion doc.** Phase F kickoff. All four `config/profiles/*.toml` files expanded from the 3-peer demo shape (sensor / controller / recorder) to the F1 6-peer shape (1 sensor / 2 controller / 3 recorder / 4 vision_capture / 5 ml_inference / 8 dashboard_feed), matching the [8-peer SYSTEM-VISION catalog](SYSTEM-VISION.md) 1:1 — peers 6 mavlink_gateway and 7 python_tooling are reserved (port 19106/19107 in hil; 10.0.0.7/.8 in sim_cloud) for F4 / F2 to slot in without renumbering. `jetson_prod.toml` is **all-SHM** at `256 × 64` per peer per ADR 0009; sideband regions ([[peers.sideband]] per ADR 0005) moved from the original controller-placeholder location to their real owners — `/rim_vision_nv12` (8 MiB) on peer 4, plus `/rim_ml_tensor_in` (16 MiB) and `/rim_ml_tensor_out` (4 MiB) on peer 5; all use the `rim_` prefix per the rename. Route table within the 2-dest-per-source cap: 1 → [2,3], 2 → [3], 4 → [5,3], 5 → [2,3]; recorder (3) is the central log + tap point seen by every active source. **Design realization**: the original plan to put recorder + dashboard_feed on UDS within `jetson_prod.toml` was reverted after discovering that `ShmRouterLink::bind_router` (line 50) silently skips non-SHM peers and `send_to_peer` (line 183) throws on unknown dests — mixing transports in one profile crashes the forward loop on the first cross-transport route hit. This is the deeper part of parked C5 ("declarative transport layer gaps") and is now documented explicitly. New operator companion doc `docs/deployment-profiles.md` (~270 lines): profile selector table, peer catalog, route topology mermaid + TOML, per-profile shape sections (jetson_prod all-SHM, x86_dev all-UDS, hil all-UDP loopback with port allocation table, sim_cloud all-UDP container subnet), **Known limitations** section explaining (a) single transport per router instance with two workarounds (multi-router topology + native SHM N-API addon) cross-referenced to parked C5, (b) 2-destination cap + first-match-wins routing cross-referenced to parked C5, (c) dashboard limitation as a consequence, (d) reason peers 6/7 are absent in F1, plus resource-name convention table covering all `rim_*` paths + operator hand-off checklist (pick profile → edit addresses → validate → install systemd units → configure peer env files → `make ci`). Test updates: `topology_loader_test::test_load_from_file` peer_count + route_count assertions bumped 3→6 + 2→4 plus peer-id-presence checks for all six F1 peers and explicit-absent for peers 6 + 7; `test_shm_ring_sizing_jetson_profile` updated to expect 6 SHM peers at 256×64 (was 3); SHM cleanup lists in `profile_switch_test.cpp` + `router_restart_test.cpp` + `fault_injection_test.cpp` extended with the new control-plane regions (`/rim_router_vision_capture`, `/rim_router_ml_inference`, `/rim_router_dashboard`); `smoke_jetson_round` + `smoke_hil_round` peer_count assertions updated. Cross-links: `docs/robotics-reference-layout.md` per-deployment profile mapping table updated with the 6-peer entries (sideband + reserved ports noted), scope-vs-deferred table row points at `deployment-profiles.md`; `ipc/MODULE.md` Deployment profiles section retitled "Phase F F1" + rewritten to reference the 6-peer expansion + cross-link, Related documents updated; `examples/bridges/README.md` connection-transport table notes the all-SHM constraint on Jetson + new doc link in Related documents. Acceptance gate (`make ci` end-to-end 28.6 s): 652 unit assertions (topology_loader 89→91) + 110 integration assertions (was 98 — profile_switch added 2 from the 6-peer + 6→6 round-trip; cleanup lists added 21 unlink calls but no new assertions; fault_injection unchanged at 34) + UDS / UDP / SHM router scenarios + `shm_leak_check.sh` **0 leaks** across the new control-plane region set. Parking discipline maintained: parked C5 is referenced four times in the new doc + once in jetson_prod.toml header + once in the route table — but the parked-review document itself is not modified; C5 reopens during the post-Phase-F walk per the established discipline. | phase-f1 |
| 2026-05-27 | E4 | **Router timestamp clock ADR (`CLOCK_MONOTONIC_RAW`) + library helper.** New `docs/adr/0010-router-timestamp-clock.md` records the decision: stamp `RouterFrame::timestamp_ns` on the forward path with `CLOCK_MONOTONIC_RAW` (slew-free, immune to NTP / `adjtime`; survives `systemd Restart=on-failure` within a boot epoch; cross-process comparable on the same host since both peer and router read the same kernel hardware counter). New library helper `ipc/src/router/timestamp.hpp` exposes `inline uint64_t router_now_ns()` wrapping `clock_gettime(CLOCK_MONOTONIC_RAW, &ts)` — peers that want to stamp their own frames with the same clock the router uses include this header (header-only, no extra link deps; layered library-side so it may not include `router_app.h`). `ipc/test/router_server.cpp` updated: dropped `using Clock = steady_clock` + `kStartTime` global + `ns_since_start` function + `<chrono>` include; now passes `router_now_ns` as the `now_ns_fn` callback to `server.run(...)`. Wire format unchanged (same field, same width, same endianness, same offset in the 64 B v2 frame); router library (`link.hpp` line 95, `shm_router_link.hpp` line 91) untouched since the clock was always a callback parameter. **Cross-host time correlation is explicitly delegated** per ADR 0010 §Out of scope: (a) application-level peers add their own PTP-anchored / sim-time / wall-clock field in their 32 B inline payload or in a sideband region per ADR 0005 — the router transports those bytes opaquely; (b) a future dedicated recorder module (the kind needed for replay per parked review C4) is the natural home for richer time semantics because its statelessness and latency requirements are relaxed relative to the router's hot path — it can afford `phc2sys` queries, `CLOCK_TAI` reads, and per-frame metadata for cross-host reconstruction. Parked review C8 single-host portion is now closed by this ADR; cross-host portion remains in the backlog as a *delegation policy*, not a deferred deliverable. Alternatives considered + rejected: (A) `steady_clock` status quo (implementation-defined — `CLOCK_MONOTONIC` on glibc, not portable; the `- kStartTime` offset destroys restart survival + cross-process comparability), (B) `CLOCK_TAI` + PTP (drags `ptp4l` / `phc2sys` host dependency into router scope; new failure modes for the hot path), (C) two-clock frame with separate publisher + router timestamps (would re-carve ADR 0008's 64 B layout, costing either inline-payload bytes or another field; the same measurement is available today by having peer and subscriber both call `router_now_ns()` and diff against `frame.timestamp_ns()`), (D) `CLOCK_BOOTTIME` (includes suspend time — irrelevant for robotics; `CLOCK_MONOTONIC_RAW` is the more conservative pick). Cross-links: `docs/robotics-reference-layout.md` "Cross-host time" operational note rewritten to "Time semantics" — lists the four properties (single-host since-boot, slew-free, restart-surviving, resets on reboot), describes the two delegation paths (app-level field, future recorder), points at ADR 0010 + `router/timestamp.hpp`; scope-vs-deferred table "Cross-host time correlation" row updated to "Beyond Phase E4 — ADR 0010 single-host, cross-host delegated, see parked C8"; vision_capture integration-pattern `timestamp_ns` row now reads "`CLOCK_MONOTONIC_RAW` — peers stamp via `router_now_ns()` for single-host comparability"; forward-references table E4 row points at ADR 0010 instead of the plan. `ipc/MODULE.md` Wire-format table `timestamp_ns` row + Related documents section both link ADR 0010 + the new header. STATUS.md Phase E E4 → `[x]`; Phase E phase-completion row → `[x]`; active phase advanced to **F** (Phase E complete). Acceptance: `make clean && make all` clean rebuild (all 38 binaries link), `make test-ipc-unit` 652/652 unchanged (assertions use literal values + `fake_now_ns()` counters — no behavioral surface), `make test-router` UDS / UDP / SHM all green (recorder CSV now contains since-boot ns values instead of since-start ns values; format and count unchanged). Parking discipline maintained: parked review C8 surfaced in the ADR with explicit closure semantics ("single-host portion closed; cross-host portion delegated"), parked C4 cross-referenced for the future-recorder rationale, but the review document itself is not modified. | phase-e4 |
| 2026-05-30 | planning | **C5 Scope C → Phase G; Scope D → C7.** Re-classification of the two C5 sub-scopes that remained parked after the 2026-05-28 Scopes A + B closure. **Scope C (per-topic routing) promoted to its own phase plan** — new `robotics-ipc-module/plans/G-declarative-routing.md` (~120 lines, mirrors the Phase E / F plan-file shape) lays out six deliverables: G1 ADR (new ADR file in `docs/adr/` recording the dispatch-key change from `source` to `(source, topic_id)` and rejecting three alternatives — separate `[[topic_routes]]` array, multi-match union, dispatch-in-topic-registry — with the keep-first-match-wins reasoning), G2 `RouteRule` surgery (`topic_id` selector + `kTopicAny` sentinel; new `make_topic_route(...)` factory alongside existing `make_route`; `route_targets_for` becomes two-argument; hot path in `link.hpp` + `shm_router_link.hpp` reads `frame.topic_id()` before dispatch — topic-less rules stay branch-light), G3 loader (`[[routes]]` gains optional `topic` field; loader resolves against `[[topics]]` ids at load time; rejects unknown topic ids with frame-aware errors; topic-less routes remain backward-compatible — opt-in per rule), G4 profile rewrites (all four `config/profiles/*.toml` gain `[[topics]]` + per-topic example routes; dashboard moves from source-only fanout to per-topic where appropriate — exact topology a G design decision; existing dashboard tap is the fallback), G5 integration tests rebuilt (new cases in `routing_test` + `topology_loader_test`; new `topic_dispatch_test` end-to-end binary; backward-compat gate via existing test cases), G6 documentation (`docs/deployment-profiles.md` per-topic section, `ipc/MODULE.md` row update, `docs/robotics-reference-layout.md` `ml_inference` per-source caveat removed, parked-review C5 marked closed). Explicit out-of-scope on Phase G plan: topic-level QoS attributes à la ROS 2, wildcard / regex topic matching, topic-driven multi-routing (first-match-wins preserved), and — critically — priority-aware QoS, which is moved out of C5 entirely (see below). The promotion rationale is recorded in the plan file's "Why this is a phase, not a sub-scope" table: Scopes A + B were close-out work (additive fields, no dispatch change); Scope C is a routing-primitive change that changes the dispatch primitive, the TOML schema, every profile, and the integration-test surface — phase-shaped, not sub-scope. **Scope D (priority-aware QoS) merged into C7** ("Real-time / production knobs"). The C7 entry header retitled to include "priority-aware QoS"; a new "Scope absorbed 2026-05-30" banner notes the merge; the existing Finding / Evidence sections extended to cover `RouterFrame::flags` priority bits being advisory-only today; Options gains a fourth bullet documenting the priority-aware QoS sub-scope and the four reasons it belongs with RT pinning rather than declarative routing — shared problem domain (latency under contention), shared hot path (`forward()` on both link types), shared systemd surface (`MemoryLock=`, `CPUAffinity=`, `LimitRTPRIO=`), shared failure signature (p99 drift, not config-shape error). Parked-review header gains a "Re-classifications (2026-05-30)" line listing both the promotion and the merge; the C5 entry's "Still parked" section replaced with a new "Re-classifications" section that documents the promotion and merge with cross-refs in both directions; Group 3 list updated (C5 entry rewritten to reflect the closed/promoted/merged state; C7 entry annotated with the absorbed-Scope-D note). Cross-links: `docs/deployment-profiles.md` Forward references row for "Per-topic routing, priority-aware QoS" split into two rows ("Per-topic routing" → **Planned (Phase G)** with link, "Priority-aware QoS" → **Parked (C7)** with link); `docs/robotics-reference-layout.md` scope-vs-deferred row updated; `ipc/MODULE.md` Related documents adds a link to the new Phase G plan; STATUS.md phase-completion table adds a Phase G row (planned, not active), backlog blurb rewritten to describe the promotion + merge. **No code changes** — this session is plan-pack hygiene only. Acceptance: `make all` clean (no source delta), `make test-router` UDS / UDP / SHM all green (regression sanity — no behavioral change), the new `plans/G-declarative-routing.md` and the parked-review revisions read cleanly. Parking discipline maintained: the two re-classifications are documented bidirectionally (Phase G plan cross-refs C5; parked C5 cross-refs Phase G; C7 explicitly absorbs Scope D with a banner; Scope D's former entry in the C5 "Still parked" section removed from that section but preserved in the new "Re-classifications" subsection so the trail is auditable). | planning |
| 2026-05-28 | F / parked-C5 A+B | **C5 Scope A + Scope B — lift 2-destination cap; add declarative topic registry.** Scope A reshapes `RouteRule` from `{ source, dest0, dest1 }` to `{ source, dest_count, std::array<uint8_t, kMaxRouteDests> dest }` with `kMaxRouteDests = 8` (matches the existing `RouteTargets::ids` width — so `ForwardResult` and the link hot paths in `link.hpp` / `shm_router_link.hpp` are unchanged). A variadic constexpr factory `make_route(source, d0, d1, …)` deduces `dest_count`, refuses zero-dest or >8-dest rules at compile time, and replaces the prior `{src, d0, d1}` positional aggregate-init at every call site (`router_client_config.h`, `shm_backpressure_test.cpp`, `burst_sensor_test.cpp`, `slow_recorder_test.cpp`, `fault_injection_test.cpp`, `routing_test.cpp`). `route_targets_for` copies `dest_count` slots; the array-tail-is-`kEndpointInvalid` contract is preserved (defensive coverage in `routing_test::test_dest_count_truncation_does_not_leak_tail`). TOML loader accepts `dest = [...]` arrays of length 1..`kMaxRouteDests`, rejects duplicates within a rule, and continues to reject self-routing per Phase D1. All four `config/profiles/*.toml` widened uniformly — every compute-side rule now ends with `dashboard_feed` (peer 8) as a trailing destination, closing the F1 dashboard limitation; route count unchanged (5 routes in `x86_dev.toml`); listing peer 8 last preserves pre-Scope-A delivery order to existing destinations. Scope B adds `ipc/src/router/topic_table.hpp` — `TopicEntry { uint16_t id; uint16_t sideband_idx; const char* name; const char* payload_class }` (free-form `payload_class`, `sideband_idx` defaults to `kSidebandIdxNone` so the registry round-trips with the `RouterFrame` "no sideband" default), `topic_by_id` / `topic_by_name` lookups (mirror `peer_by_*`). `RouterTopology` gains `const TopicEntry* topics` + `size_t topic_count` (default-empty; every existing aggregate-init still compiles). TOML loader parses an optional `[[topics]]` section: `id` (required u16, unique), `name` (required, unique, ≤63 bytes), optional `payload_class` (≤63 bytes), optional `sideband_idx`. `x86_dev.toml` ships a 4-topic worked example (`imu_proprio` / `controller_command` / `vision_frame` + sideband 0 / `ml_result` + sideband 1); other profiles intentionally omit `[[topics]]` to demonstrate it is optional. Router itself never consults `topics_` at forward time — consumers opt in to validation. Scope C (per-topic routing) and Scope D (priority-aware QoS) remain parked. Tests: `routing_test` 60/60 (+5 for Scope A: fan-out-to-3, fan-out-to-`kMaxRouteDests`, mixed-width rules, `dest_count` truncation safety, `make_route` constexpr deduction), `topology_loader_test` 126/126 (+5 for Scope A: 3-dest TOML, 8-dest TOML, >8-dest rejected, duplicate-dest rejected, new dashboard-tap + topic-registry assertions in `test_load_from_file`), new `topic_registry_test` 42/42 (absent-section / 3-topic round-trip / lookup by id and name / id=0 legal / move-stable interned pointers / 7 loader-error cases), `profile_switch_test` 43/43 (comment-only update for wider fan-out). Docs: `robotics-ipc-module/plans/post-phases-robotics-review.md` — C5 entry gains "Status update" banner, "Closure — Scope A" + "Closure — Scope B" sections, "Still parked" call-out for C + D; review header notes the partial closure. `docs/deployment-profiles.md` — Known limitations §2-destination cap + §Dashboard limitation marked **closed 2026-05-28** with historical notes preserved; new §Topic registry section with full schema table; Forward references row split into closed (A+B) vs parked (C+D); route topology mermaid + TOML snippet + intro updated for the dashboard-tap-on-every-rule shape. `ipc/MODULE.md` related-documents bullet pointing at `topic_table.hpp`. STATUS.md active phase line updated. Acceptance: `make ci` end-to-end clean — 752 unit assertions across 9 binaries (added `topic_registry_test` to the suite list and to `test-ipc-unit`), 154 integration assertions (was 110 from F2 — `topology_loader_test` grew from 108 to 126, `topic_registry_test` is 42 new), UDS/UDP/SHM router scenarios all green, `shm_leak_check.sh` clean delta=0 across the full pass, `examples/bridges/python_peer/smoke.sh` green (5 frames forwarded + decoded byte-exact). Parking discipline maintained: closure notes live alongside the original C5 entry; C5 Scopes C and D explicitly remain parked. | parked-c5 A+B |
| 2026-05-25 | D1 | **Unit suites + self-routing rejection.** Four new test binaries: `datagram_seq_test` (296 assertions covering `SourceSeqTracker` first-observation, in-order streams, gap accounting, duplicate vs. out-of-order classification, 2³² wrap with and without pre-wrap gap, independent-source isolation, reset/clear, LVC integration under 10% packet loss, template `N` bounds), `routing_test` (28 assertions for `route_targets_for` empty-rules / unknown-source / unicast / broadcast / first-match-wins / dest1==0 sentinel / `kEndpointInvalid` source / `ForwardResult` truthiness), `resolver_test` (16 assertions for `peer_id_from_recv<Uds>` strcmp match + unknown + empty + prefix non-match, `peer_id_from_recv<Udp>` port match + unknown + host-byte-order ignored, mixed-transport topology branch isolation), `cli_args_test` (13 assertions locking `log_path_for_role` arity — controller `argv[5]`/`argv[3]`/fallback, recorder `argv[4]`/`argv[3]`/fallback, recorder does NOT consume `argv[5]`, unknown role short-circuits to `""`, null-fallback safety). New library header `ipc/src/router/source_seq_tracker.hpp` (per-source uint32 seq tracker, modular-arithmetic wrap, 2³¹-wide forward/backward classification windows; pairs with `LastValueCache` on subscriber read path). New test-only helper `ipc/test/router_cli_args.hpp` (callback-based fallback so the same function is used by both `router_client.cpp` and the regression test). Topology loader now rejects self-routing (`route.source` ∈ `{dest0, dest1}`) with frame-aware error text — `topology_loader_test` grows +2 assertions (73 total). Aggregate post-D1: `make test-ipc-unit` **642/642 assertions across 8 binaries** (163 frame + 73 loader + 26 LVC + 27 SHM backpressure + 296 datagram_seq + 28 routing + 16 resolver + 13 cli_args); `make test-ipc` UDP 720k + UDS 795k + SHM 8.0M trips/5s; `make test-router` all 3 transports green (no demo CLI regression). | phase-d1 |
